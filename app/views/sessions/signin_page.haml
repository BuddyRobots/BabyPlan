- content_for :head do
  = javascript_include_tag "pages/sessions/signin-bundle"
  = stylesheet_link_tag "pages/sessions/signin-bundle", :media => "all"

/ - content_for session.haml yield under the wrapper 有名字时必须指定名称，例如上面的:head，　无名字时可以不用写
.content-area
  .input-div
    %img.logo{src: asset_path("logo1.png")}
    .input-group
      %span.input-group-addon.miniphoto
      %input.form-box.f16{placeholder: "请输入您的手机号", type: "text"} 
